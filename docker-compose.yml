services:
  backend:
    build: .
    ports:
      - "7070:7070"
    env_file:
      - backend/.env
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    restart: always

# Optional: Add database/redis if running locally via docker
